# Full rebuild 20260923a — Minerar PARÁ

## Brand
- UI name: **Minerar PARÁ**
- Accent `#F5A623`, bg `#0b1220`
- Logo escavadeira crop+1024 + icons regenerados
- Caixa Minerar Seguro / Empréstimos Minerar Seguro
- Greeting: `Olá, [apelido]`
- Cache: `20260923a` (sw + assets)

## Pillars
1. **Marketplace (inicio)** — OLX 1:1: pin+cidade, bell, tabs underline, search, 4 quick cards, banner, 2-col grid 16:9+heart, FAB, detail page `lote-detalhe.html` with round **Voltar**
2. **Chat** — WhatsApp 1:1: list chrome, chips, Arquivadas, rows 56px, thread Voltar+calls toast, orange sent bubbles, compose + bottom sheet, mic hold
3. **Bank (financeiro)** — Nubank 1:1 orange strip: Olá, saldo block, 5 circles (Pix/Depositar/Pagar/Pegar emprestado Novo/Ajuda), cards, views with round Voltar (emprestimos/simular/pix/depositar/pagar/extrato/sacar)

## Map
- **No embed**. `mapa.html` + `mapa-native.js`: Android `geo:`, iOS `maps:`, fallback Google Maps search Parauapebas, PA

## SQL for parent
Apply **`sql/33-storage-user-paths.sql`** after 32 (idempotent).
- chat-midia mutate only under `{auth.uid()}/...`
- emprestimo-docs already uid-prefixed; policies reaffirmed
- Front chat upload path updated to `uid/pasta/...`

## Isolation checklist (A vs B)
- A cannot read B caixa_saldos / movimentos / emprestimos (RLS own)
- A cannot read B chat unless participant
- A cannot list B storage objects outside own folder (after SQL 33)
- Admin-as-user modo UI: JWT still auth.uid() of admin — do not impersonate DB as other user; modo toggle only changes chrome (preserved)

## Position checklist
- [ ] OLX: pin left / bell right / tabs / search / 4 squares / banner / 2-col / FAB
- [ ] WA: ⋮ left / cam+plus right / Conversas / chips / Arquivadas / avatar56 rows / Voltar thread / composer + sheet
- [ ] Nubank: orange strip / Olá / black saldo / 5 circles order / Meus cartões / list chevrons / Empréstimos screen
- [ ] Map opens native (no Leaflet)
- [ ] Foreign resource 403 (cross-user storage / caixa)

