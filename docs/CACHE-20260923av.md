# Cache bump 20260923av — unicidade nome+apelido

## Segurança / chat
- Par **nome + apelido** único (case-insensitive, trim) em `usuarios`.
- SQL `43-nome-apelido-unique.sql`: índice único + RPC `nome_apelido_disponivel` + trigger PT.
- Validação no cadastro (`auth.js`) e no perfil (`perfil.js`); mensagem clara em PT.

## Hard reset
`ASSET_V` + SW `CACHE` = **20260923av** + skipWaiting / clientsClaim + wipe de caches no boot.
