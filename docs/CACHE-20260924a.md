# Cache bump 20260924a — Fale conosco só no Perfil

- Botão «Fale conosco» vive apenas em `perfil.html` (`#card-fale-conosco`, card dentro do conteúdo, largura total, 48px).
- `suporte.js` não injeta mais botão/FAB em nenhuma página; o modal do Robô Minera é criado sob demanda ao tocar no card.
- Admin (página admin / modo admin): card oculto.
- Remove CSS de FAB/pill do header.

## Hard reset
`ASSET_V` + SW `CACHE` = **20260924a** + skipWaiting / clientsClaim + wipe caches no boot.
