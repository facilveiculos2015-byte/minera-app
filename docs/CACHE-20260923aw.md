# Cache bump 20260923aw — nome+apelido unique + layout taps

## 1) Unicidade nome + apelido
- SQL `43-nome-apelido-unique.sql`: índice unique lower(trim(nome))+lower(trim(apelido)), RPC `nome_apelido_disponivel`, trigger PT.
- Validação em cadastro (`auth.js`) e perfil (`perfil.js`).
- Mensagem: «Já existe alguém com este nome e apelido. Escolha outro apelido.»

## 2) Layout / taps mobile
- FAB «Fale conosco» no mobile sempre flutuante (bottom-left), não no header com Sair.
- Admin sticky «Voltar ao Admin»: padding safe-area + z-index; Perfil topbar desce abaixo da barra.
- Sair visível no Admin e no Perfil; z-index alto; Fale oculto no Admin.

## Hard reset
`ASSET_V` + SW `CACHE` = **20260923aw** + skipWaiting / clientsClaim + wipe caches.
